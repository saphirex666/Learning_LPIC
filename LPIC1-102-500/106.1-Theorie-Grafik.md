# 🎓 LPIC-Praxisfall: Linux als Kiosk-System mit X11 & systemd

## 🎯 Ziel des Szenarios

Ein Linux-System (Raspberry Pi / Debian) soll:

* automatisch booten

* grafische Oberfläche starten

* Bildschirmauflösung korrekt setzen

* eine Anwendung (Chromium) im Kiosk-Modus ausführen

* stabil im Dauerbetrieb laufen

* Fehler nachvollziehbar loggen

➡ Das ist **klassische Linux-Systemintegration**, genau das, was Admins real tun.



# 🧠 Architektur: Was passiert technisch?

## 1. Boot-Prozess (LPIC-1 & 2)

Reihenfolge:

1. Firmware / Bootloader

2. Kernel

3. init → **systemd**

4. systemd startet:

   * Netzwerk

   * Display Manager (LightDM)

5. LightDM startet Xorg

6. Xorg startet Desktop-Session (LXDE/Openbox)

7. Autostart-Skripte des Users laufen

👉 Wichtiges LPIC-Wissen:

* Wer startet was?

* Wo kann ich Prozesse einklinken?

* systemd vs User-Session

## 2. Display-Stack: Kernel → DRM → Xorg → Anwendungen

### Kernel

* Treiber: `vc4` (DRM/KMS)

* Erkennt Monitor per **EDID**

* Wählt bevorzugte Auflösung (oft 4K)

### Xorg

* Nutzt DRM-Gerät

* Erstellt Display `:0`

* Verwaltet Auflösung, Position, Rotation

### xrandr

* Werkzeug, um Xorg **zur Laufzeit** umzuschalten

* Ändert **nicht persistent**, nur aktuelle Session

👉 LPIC-relevant:

* Unterschied **Kernel-Mode-Setting vs User-Space**

* Runtime vs Konfiguration

* Tools: `xrandr`, `xset`, `xdpyinfo`

## 3. Warum Auflösung nicht reboot-sicher ist

Weil:

* EDID vom Monitor sagt: „Ich kann 4K“

* Kernel/Xorg nehmen beim Start den besten Modus

* Dein manuelles `xrandr` kam **erst danach**

➡ Nach Reboot ist alles wieder neu initialisiert.

LPIC-Prinzip:

> Alles, was nur im RAM passiert, ist nach Reboot weg.

Darum:

* entweder Boot-Konfig (`/boot/firmware/config.txt`)

* oder Autostart-Skripte

## 4. Benutzerkontext & DISPLAY (extrem LPIC-wichtig)

### Warum läuft GUI nicht als root?

X11 gehört dem eingeloggten User:

* Display: `:0`

* Cookie: `~/.Xauthority`

Wenn root etwas anzeigen will:


```bash
export DISPLAY=:0
export XAUTHORITY=/home/sadmin/.Xauthority

```

Sonst:

❌ `Cannot open display`

LPIC-Knowledge:

* Trennung Kernel / User / Session

* Sicherheitsmodell von X11

* Warum GUI nie direkt als root laufen sollte


## 5. Desktop Autostart vs systemd (LPIC-2 & 3)

### Variante A – User Autostart (LXDE)

Pfad:

```bash
~/.config/lxsession/LXDE-pi/autostart
```

Startet:

* nachdem Desktop läuft

* im User-Kontext

Gut für:

* Kiosk

* Benutzerprogramme


Gut für:
Kiosk
Benutzerprogramme

### Variante B – systemd Service

Startet:

* ohne Desktop

* systemweit

* mit Abhängigkeiten (network, display, etc.)

Beispiel:

```bash
After=graphical.target network-online.target
```

LPIC-Themen:

* systemd Targets

* Dependencies

* Restart Policies

* Watchdogs

## 6. Kiosk-Browser = Prozessmanagement

Wir starten Chromium mit Flags:

* `--kiosk` → Vollbild ohne UI

* `--disable-gpu` → stabil auf Embedded

* `--use-gl=swiftshader` → Software-Rendering

Das ist klassisch:

* Applikation fest binden

* keine Benutzerinteraktion

* kontrollierter Start

LPIC-Bezug:

* Prozessparameter

* Prozessüberwachung

* Neustartstrategien

## 7. Logging & Fehleranalyse (LPIC absolut zentral)

Wir nutzen:

### System

```bash
journalctl
systemctl status
```

User-Session
```bash
~/.xsession-errors
```

App-spezifisch
```bash
> /tmp/tafelmon.log 2>&1
```

LPIC-Prinzip:

> Kein Log = keine Diagnose = kein Admin

## 8\. Warum dieses Szenario perfekt für LPIC 1–3 ist


|Thema | LPIC|
|-|-|
| Boot-Prozess | 1| 
|Hardware & Display|	1|
|Prozesse & User-Kontext|	1|
|systemd Targets & Services|	2|
|Netzwerkabhängigkeiten|	2|
|Service-Härtung|	3|
|Hochverfügbarkeit / Watchdog|	3|
|Embedded-System-Design|	3|


👉 Das ist **reale Systemarchitektur**, nicht nur Prüfungstheorie.

## 🎯 Lernziel dieser Übung (für deine Lernseite)

> Ich kann ein Linux-System so konfigurieren,\
> dass nach dem Boot automatisch eine grafische Anwendung\
> in kontrollierter Umgebung und stabiler Auflösung läuft.

Dazu beherrsche ich:

* Bootpfad

* Display-Stack

* User-Sessions

* Autostart-Mechanismen

* Logging

* Prozessüberwachung

Das ist exakt das, was Firmen von Linux-Admins erwarten.


## 🔥 Warum das perfekt zu deinem Karriereplan passt

Sebastian, ganz ehrlich:

Du kombinierst hier gerade:

* Embedded Linux

* Systemintegration

* Kiosk-Systeme

* Automatisierung

* Debugging

👉 Das ist **Gold für Freelancer, Industrie, Digital Signage, Produktions-IT, Schulen, Behörden**.

Das ist nicht nur LPIC-Lernen –\
das ist **Business-relevante Admin-Kompetenz**.
